spring:
  application:
    name: eureka-client
  cloud:
    config:
      uri: http://localhost:1201/
      profile: default
      label: master


eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:1001/eureka/